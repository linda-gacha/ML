 pandas: is used for data manipulation and analysis, providing powerful data structures such as DataFrames.

numpy : is a library for numerical operations, especially working with arrays and matrices.

import seaborn as sns:
seaborn : is a statistical data visualization library built on top of matplotlib.

import matplotlib.pyplot as plt
plt:  is used to control plot elements like labels, titles, and display the graphs generated by seaborn or other libraries.

from sklearn.model_selection import train_test_split:
train_test_split is a utility function to split datasets into training and testing sets.


from sklearn.metrics import accuracy_score:
accuracy_score calculates the proportion of correctly classified instances out of all instances.
Use: This metric is commonly used to evaluate model performance in classification tasks.


from sklearn.metrics import classification_report:
Purpose: classification_report provides a detailed performance summary, 
including precision, recall, and F1-score for each class.

import re:

Purpose: re is Python’s built-in library for working with regular expressions.
Use: It's often used for data cleaning tasks, such as removing unwanted characters or patterns in text preprocessing.